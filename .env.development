# Vite dev server proxies /api → this URL (see vite.config.ts).
VITE_BACKEND_URL=https://dukaplusbackend-production.up.railway.app

# Local backend (full latest API — accounting bundle, payroll profile, TRA demo):
# VITE_BACKEND_URL=http://127.0.0.1:8000
